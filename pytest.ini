[pytest]
pythonpath = .
markers =
    unit: snelle unit-tests zonder externe afhankelijkheden (default aan)
    integration: tests die een live Supabase vereisen (alleen actief met RLS_TEST_ENABLED=1)
